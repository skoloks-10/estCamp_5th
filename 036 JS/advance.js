export function calculateMod(num1, num2) {
  return num1 % num2;
}
export function calculatePow(num1, num2) {
  return Math.pow(num1, num2);
}
export function calculateSqrt(num) {
  return Math.sqrt(num);
}
